
#include <stdio.h>
#include "module/command/command.h"

int main(int argc, char argv[]) {

    // 判断是否登录成功
    if (!command_login_success(argc, argv)) {

        return 0;
    }

    // 判断是否
    if ()


        return 0;
}
